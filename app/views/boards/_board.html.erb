<div id="<%= dom_id board %>">

  <form>
  <div class="form-group row">
    <label for="generation" class="col-sm-2 col-form-label">Generation</label>
    <div class="col-sm-10">
      <input type="number" readonly class="form-control" id="generation" value="<%= board.generation %>">
    </div>
  </div>
  <div class="form-group row">
    <label for="rows" class="col-sm-2 col-form-label">Rows</label>
    <div class="col-sm-10">
      <input type="number" readonly class="form-control" id="rows" value="<%= board.rows %>">
    </div>
  </div>
    <div class="form-group row">
      <label for="rows" class="col-sm-2 col-form-label">Cols</label>
      <div class="col-sm-10">
        <input type="number" readonly class="form-control" id="cols" value="<%= board.cols %>">
      </div>
    </div>
</form>

  <%# board.data.scan(/.{#{board.cols}}/).each do |row| %>
<!--    <%#= row.gsub("1", "*").gsub("0", ".") %><br>-->
  <%# end %>

</div>
